# Build output
bin/
obj/
artifacts/

# Rider / VS
.vs/
.idea/
*.user

# Dev tunnel / local secrets
appsettings.Local.json
*.env
.uipath/.skills/
.worktrees/
.repowise/

# CocoIndex Code (ccc)
/.cocoindex_code/

# Agent tool scratch (Qwen tmp working files)
.qwen/tmp/

# Vendored-skill divergence mirror. It mirrors .agents/skills/LOCAL-DIVERGENCE-uipath-rpa.md
# inside the vendored tree so the tree is self-describing, but it must never be tracked:
# its "Claim changed" column quotes the XAML-first phrases, so tracking it would add noise
# to the documented `git grep` verification in the divergence file.
.agents/skills/uipath-rpa/LOCAL-DIVERGENCE.md
